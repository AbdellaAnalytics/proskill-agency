# السيو — ما تم وما يجب أن تفعله

## 🔴 الباج الذي كان يمنع ظهورك في جوجل نهائياً

كل صفحة منتج كانت تقول لجوجل:
```html
<link rel="canonical" href="https://store.proskillagency.com/" />
```
أي: "أنا نسخة مكررة من الصفحة الرئيسية — لا تفهرسني."

**النتيجة:** مستحيل لأي منتج أن يظهر في نتائج البحث، مهما فعلت.

الآن كل صفحة منتج تُقدَّم بـ canonical وعنوان ووصف وصورة خاصة بها — من السيرفر، قبل تشغيل أي جافاسكريبت (وهذا ما يقرأه الزاحف).

---

## ما تم في هذه النسخة

| البند | قبل | بعد |
|---|---|---|
| canonical لصفحة المنتج | يشير للرئيسية ❌ | يشير للمنتج ✅ |
| عنوان صفحة المنتج | اسم المتجر العام | اسم المنتج |
| وصف صفحة المنتج | نص عام واحد للكل | وصف المنتج (مقصوص عند حدود كلمة) |
| صورة المشاركة (فيسبوك) | لا شيء | صورة المنتج |
| Product schema | ❌ | ✅ سعر + توفر → يظهران في جوجل |
| sitemap | 6 صفحات + دومين خاطئ | ديناميكي + كل المنتجات + الدومين الصحيح |
| og:image للرئيسية | ❌ مفقود | ✅ 1200×630 |

---

## 🔴 عليك أنت (أهم من أي كود)

### 1. Google Search Console — الخطوة الحاسمة
- `search.google.com/search-console` → Add property → `store.proskillagency.com`
- التحقق: اختر **DNS** (المطور يضيف سجل TXT) أو **HTML tag**
- بعد التحقق: **Sitemaps** → اكتب `sitemap.xml` → Submit
- ثم **URL Inspection** → الصق رابط منتج → **Request Indexing**

بدونها قد يستغرق جوجل شهوراً ليجدك.

### 2. رابط من موقعك القديم ← أسرع مكسب
`www.proskillagency.com` له عمر وثقة عند جوجل. زر "المتجر" منه يمرّر جزءاً من هذه الثقة. هذا أقوى من أي تحسين كود.

### 3. الأوصاف العربية
منتج بوصف فارغ = وصف عام في جوجل + لا يظهر في البحث الداخلي.
الأوصاف جاهزة في `ProSkill_All_Products.md`.

### 4. اختبر المشاركة
- فيسبوك: `developers.facebook.com/tools/debug` → الصق رابط منتج → Scrape Again
- جوجل: `search.google.com/test/rich-results` → الصق رابط منتج

---

## ⚠️ حد معماري يجب أن تعرفه

الموقع **يُبنى في المتصفح (SPA)**. حقن الميتا يحل مشكلة الفهرسة والمشاركة، لكن **نص الصفحة** لا يزال يحتاج جوجل أن يشغّل الجافاسكريبت ليراه — أبطأ وأقل ثقة من موقع يُبنى في السيرفر.

الـ Product schema المحقون يعطي جوجل البيانات المهمة (اسم، سعر، توفر، وصف) **بدون** جافاسكريبت — وهذا يغطي أهم جزء.

**متى تعيد النظر:** فقط لو أصبح البحث المجاني مصدر دخلك الأساسي. حالياً الإعلانات + الروابط المباشرة `/p/` أسرع طريق للبيع.

---

## التوقعات الواقعية

موقع جديد يحتاج **أسابيع إلى شهور** ليُفهرس ويترتب. هذا طبيعي. الترتيب:
1. Search Console + تسليم sitemap (اليوم)
2. رابط من الموقع القديم (اليوم)
3. الأوصاف (هذا الأسبوع)
4. انتظار + محتوى + روابط (مستمر)

---

# السيو الداخلي — الدفعة الثانية

## 🔴 أكبر فرصة كانت ضائعة: الأقسام بلا صفحات

أقسامك كانت **فلاتر في المتصفح فقط** — بلا رابط. النتيجة:
- لا توجد صفحة يمكن أن ترتّب على "اشتراكات تصميم" أو "أدوات ذكاء اصطناعي"
- أزرار الأقسام كانت `<div onClick>` — **الزاحف لا يراها ولا يستطيع المرور منها**

### الآن
- كل قسم له صفحة: `store.proskillagency.com/c/design-creative-a1b2c3`
- عنوان ووصف و canonical خاص بها (من السيرفر)
- أزرار الأقسام أصبحت `<a href>` حقيقية → جوجل يمشي عليها ويكتشف الصفحات
- الأقسام مُدرجة في الـ sitemap
- الرابط يتحدث عند الفلترة → العميل يقدر يشارك "قسم التصميم"

**ملاحظة تقنية:** الـ slug مُشتق في الكود من الاسم + جزء من الـ id — **بلا عمود في الداتابيز**، فلا migration ولا مخاطرة كسر الاستعلام.

---

## Schema المضاف

| النوع | أين | الفائدة |
|---|---|---|
| BreadcrumbList | صفحات المنتجات + الأقسام | يظهر المسار تحت النتيجة بدل الرابط الخام |
| ItemList | صفحات الأقسام | جوجل يفهم ما بالصفحة |
| FAQPage | صفحة الأسئلة | **قد تظهر الإجابات مباشرة في نتائج البحث** |
| Product + Offer | صفحات المنتجات | السعر والتوفر في النتيجة |

---

## HTML دلالي
- `<main>` — يحدد المحتوى الفعلي مقابل الهيدر/الفوتر
- `<nav aria-label="الأقسام">` — يوضح أن هذه روابط تنقّل

## ما كان سليماً بالفعل (فحصته)
- `alt` على كل صورة منتج ✅
- `loading="lazy"` ✅
- هيكل العناوين H1 → H2 → H3 ✅
- OnlineStore + Organization + SearchAction schema ✅

---

## اختبر بعد الرفع
- `search.google.com/test/rich-results` → الصق رابط منتج → لازم يظهر Product + Breadcrumb
- نفس الأداة على `/faq` → لازم يظهر FAQPage
- الصق رابط قسم → لازم يظهر ItemList + Breadcrumb

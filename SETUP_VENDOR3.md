# ربط الفيندور الثالث (vex)

## متغيرات Vercel
| المتغير | القيمة |
|---|---|
| `VEX_API_URL` | `https://njhtqanmbdpvywosnowd.supabase.co/functions/v1/reseller-api` |
| `VEX_API_KEY` | مفتاحك (Bearer) |

⚠️ **بدون `?action=...`** — الكود يضيفها. ولا شرطة مائلة في النهاية.

ثم **Redeploy**.

## الاستخدام
الداش بورد → **🏪 استيراد من الفيندور**
- يعرض كتالوجهم مع **تكلفتك** ورصيدك
- اختر منتجاً → حدّد هامشك (30/40/50/70%) → **استيراد**
- المنتج يظهر في **الموقع والبوت** معاً

---

## 🛡️ لماذا هذا الربط أأمن من السابقين؟

هذا الفيندور يدعم **Idempotency**: كل طلب يحمل `external_order_id` فريداً (رقم طلبنا).
إعادة المحاولة بنفس الرقم **تُعيد التسليم الأصلي** بدل خصم جديد.

### الفرق العملي

| الحالة | Shop Bot / Subnova | vex |
|---|---|---|
| الشبكة تقطع أثناء الشراء | ❌ لا نعيد المحاولة (خطر خصم مزدوج) → الطلب معلّق | ✅ نعيد مرة واحدة بأمان → العميل يستلم |
| رفض نظيف (رصيد/مخزون) | لا خصم | لا خصم (يُسترد تلقائياً) |
| رد غير مفهوم | — | ⚠️ لا نسلّم شيئاً · تنبيه فوري لك |

### قواعد صارمة مطبَّقة
1. **إعادة محاولة واحدة فقط**، وفقط لـ `vex`، وفقط عند خطأ شبكة.
2. أخطاء `401/402/409/429` تُعامل كرفض نظيف — **لا إعادة محاولة**.
3. إذا لم نفهم شكل الأكواد في الرد → **لا نسلّم كوداً مخترعاً**، بل نُعلمك للتدخل.
4. الاستيراد يرفض سعراً أقل من تكلفتك.

---

## نقطتان تحتاج تأكيدهما من الفيندور
1. **الكمية > 1:** كيف يعود حقل `data`؟ (الكود يقبل: نص بأسطر · مصفوفة نصوص · مصفوفة كائنات)
2. **`manual_delivery`:** ما قيمة `status`؟ (الكود يعتبر `pending/processing/manual/queued` = تفعيل يدوي)

الكود **يتعامل مع كل الاحتمالات بأمان** الآن، لكن التأكيد يسمح بتبسيطه لاحقاً.

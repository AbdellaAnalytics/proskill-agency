# كوبون على منتج معيّن

## ⚠️ شغّل SQL أولاً — قبل رفع الكود

**Supabase → SQL Editor → New query** → الصق محتوى `coupon_product.sql` → **Run**

بدونه: الكوبونات ستعمل كما هي (خصم على كل المنتجات)، وسيظهر تحذير في الداش بورد.

## ترتيب التنفيذ (مهم)
1. **شغّل `coupon_product.sql`**
2. ارفع الكود
3. جرّب

لو رفعت الكود قبل الـ SQL، الداش بورد سيعمل لكن اختيار المنتج لن يُحفظ.

---

## الاستخدام
🎟️ أكواد الخصم → **＋ كود جديد** → ستجد قسم **المنتج**:

- **🛍️ كل المنتجات** ← الوضع الافتراضي (نفس السلوك القديم)
- **🎯 منتج واحد فقط** ← اكتب اسم المنتج → اختره من القائمة

الكوبونات القديمة لن تتأثر — تبقى على "كل المنتجات".

---

## ماذا يرى العميل؟
لو استخدم كوداً مخصصاً لـ Adobe على منتج آخر:
> هذا الكود يعمل على "Adobe Creative Cloud Pro" فقط

رسالة واضحة تخبره أين يستخدمه — بدل رفض غامض.

---

## 🔒 الأمان
- الفحص كله **داخل قاعدة البيانات** — لا يمكن التحايل من المتصفح
- الدالة القديمة (بثلاث معاملات) **تُحذف**. لو تُركت، أي نداء بثلاث معاملات كان سيتجاوز فحص المنتج تماماً — أي أن الميزة كانت ستتحول إلى ثغرة
- **عدم تمرير المنتج = رفض** للكوبون المقيّد. الغياب ليس ثغرة تحايل
- المعاينة (قبل الدفع) والشراء الفعلي **يمرران نفس المنتج** — فلا يَعِد المتجر بخصم يرفضه الدفع

---

## استخدامات عملية
| الحالة | الإعداد |
|---|---|
| إطلاق منتج جديد | كود 20% على المنتج الجديد فقط |
| تصريف مخزون راكد | كود على المنتج البطيء فقط |
| شراكة مؤثّر | كود باسمه على المنتج الذي يروّجه |
| تعويض عميل | كود مرة واحدة على منتجه فقط |

---

## قيود يجب أن تعرفها (وجدتها أثناء المراجعة)

**1. الكوبون لا يُعدَّل بعد الإنشاء**
هذا تصميم قديم في المتجر — كل حقول الكوبون كذلك (القيمة، الانتهاء…). تقدر **تقفله** أو **تمسحه** فقط.
لو اخترت المنتج غلط: امسح الكود وأنشئه من جديد. الأكواد رخيصة الإنشاء.

**2. حذف المنتج يحذف كوبوناته**
`on delete cascade` — لو مسحت منتجاً، الكوبونات المخصصة له تُحذف معه.
هذا **مقصود**: البديل (تحويلها لكوبون عام) يعني خصماً مفاجئاً على المتجر كله.

**3. لو رفعت الكود قبل الـ SQL**
الكوبونات **تتوقف بصمت** والعميل يدفع كامل السعر.
الفشل يذهب في **الاتجاه الآمن** (لا خصم) عمداً — البديل كان خصماً على منتج خاطئ، وهذا أسوأ.
**لذلك: SQL أولاً.**

**4. بعد تشغيل الـ SQL**
لو ظهر تحذير "شغّل coupon_product.sql" رغم أنك شغّلته، انتظر دقيقة (Supabase يحدّث تعريف العلاقات) ثم حدّث الصفحة.

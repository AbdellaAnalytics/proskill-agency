# أكواد الخصم

## التشغيل
شغّل `store_coupons.sql` في Supabase → ثم ارفع الكود → الداش بورد → **🎟️ أكواد الخصم**

## إنشاء كود
| الحقل | المعنى |
|---|---|
| الكود | حروف إنجليزية/أرقام (3–24) — مثل `WELCOME10` |
| النوع | نسبة % أو مبلغ ثابت $ |
| أقصى خصم | يحدّ من كوبونات النسبة (مثلاً 20% بحد أقصى $5) |
| الحد الأدنى للطلب | لا يُقبل الكود تحت هذا المبلغ |
| أقصى استخدامات | إجمالي (فارغ = بلا حد) |
| تاريخ الانتهاء | اختياري |

افتراضياً: **كل بريد يستخدم الكود مرة واحدة**.

---

## الأمان — لماذا لا يمكن التلاعب بالخصم؟

| محاولة | الحماية |
|---|---|
| المتصفح يرسل خصماً مزيّفاً | لا نستقبل مبلغاً من المتصفح إطلاقاً — **الكود فقط**، والخصم يُعاد حسابه في `/api/checkout` |
| تخمين الأكواد | جدول `coupons` مقفول بـ RLS بلا سياسات: مفتاح المتصفح لا يقرأه |
| كوبون 100% لطلب مجاني | الدالة تمنع نزول الإجمالي تحت \$1 |
| استهلاك كود بلا دفع | `consume_coupon` يُستدعى **فقط** بعد تأكيد الدفع الموقّع |
| استخدام الكود مرتين | فحص `max_uses_per_email` على الطلبات **المدفوعة** فقط |
| كوبون 99% من الداش بورد | الحد الأقصى المسموح 90% |
| نداء callback مكرر | الاستهلاك داخل الفرع الذي ينتقل مرة واحدة فقط |

## مثال
```sql
insert into coupons (code, type, value, min_order_usd, max_discount_usd, max_uses, expires_at)
values ('WELCOME10', 'percent', 10, 3, 5, 100, now() + interval '30 days');
```

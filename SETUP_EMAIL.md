# تفعيل الإيميل والإشعارات

## 1) Resend (إرسال الإيميلات للعملاء)

1. سجّل على https://resend.com (مجاني: 3000 إيميل/شهر · 100/يوم)
2. **Domains → Add Domain** → اكتب `proskillagency.com`
3. Resend هيديك 3 سجلات DNS → ضيفهم عند مزوّد الدومين بتاعك → **Verify**
4. **API Keys → Create** → انسخ المفتاح (يبدأ بـ `re_`)

### متغيرات Vercel
| المتغير | القيمة |
|---|---|
| `RESEND_API_KEY` | `re_...` |
| `EMAIL_FROM` | `ProSkill <orders@proskillagency.com>` |
| `ADMIN_EMAIL` | بريدك لاستقبال إشعارات الطلبات |

> **قبل توثيق الدومين** يمكنك التجربة بـ:
> `EMAIL_FROM=ProSkill <onboarding@resend.dev>` — لكنه يرسل لبريدك أنت فقط.

## 2) إشعارات تيليجرام (لك أنت)

| المتغير | القيمة |
|---|---|
| `BOT_TOKEN` | نفس توكن البوت |
| `ADMIN_TELEGRAM_ID` | رقم حسابك (من @userinfobot) |

## 3) بعد الإضافة
**Vercel → Deployments → ⋯ → Redeploy**

---

## ما الذي يحدث الآن مع كل طلب؟

| الحالة | العميل يستلم | أنت تستلم |
|---|---|---|
| ✅ تسليم فوري | إيميل بالأكواد | تيليجرام + إيميل |
| ⏱️ تفعيل يدوي | إيميل "تم الدفع، ننتظر التفعيل" | 🔔 تيليجرام + إيميل |
| 🚨 فشل التسليم | إيميل "نراجع طلبك، لا تدفع مجدداً" | 🚨 تنبيه عاجل |

**مبدأ مهم:** فشل الإيميل **لا يوقف التسليم أبداً** — الطلب يُسلَّم في قاعدة البيانات أولاً،
ثم تُرسل الإشعارات. أي فشل يُسجَّل في اللوجز فقط.

## الصفحات الجديدة
- `/track` — تتبع الطلب (رقم الطلب + البريد)
- `/faq` · `/terms` · `/privacy` · `/refund`
